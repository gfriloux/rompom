.direnv
.envrc
target
result

# Scratch: design handoffs, notes, work output. Never committed.
tmp/

# rompom runtime output, written in the working directory it runs from.
*.state.yml
*.run.yml
*.debug.log
